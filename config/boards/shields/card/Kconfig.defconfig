
config ZMK_BLE
	default y

config ZMK_USB
	default y

if SHIELD_CARD_LEFT

    config ZMK_KEYBOARD_NAME
        default "card"

    config ZMK_SPLIT
	default y

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y

endif

if SHIELD_CARD_LEFT || SHIELD_CARD_RIGHT

    config ZMK_SPLIT
        default y

endif
